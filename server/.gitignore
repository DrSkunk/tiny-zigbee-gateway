# dependencies
node_modules

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Scheduler config will be generated on the first run
schedulerConfig.json

# Server config must be made manually
serverConfig.json